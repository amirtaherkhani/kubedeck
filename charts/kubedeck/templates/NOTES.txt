KubeDeck has been installed.

{{- if .Values.ingress.enabled }}
Configured hosts:
{{- range .Values.ingress.hosts }}
  {{- if $.Values.ingress.tls }}
  https://{{ .host }}
  {{- else }}
  http://{{ .host }}
  {{- end }}
{{- end }}
{{- else }}
Forward the Service locally:
  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "kubedeck.fullname" . }} 3000:{{ .Values.service.port }}

Then open:
  http://127.0.0.1:3000
{{- end }}

The initial administrator is loaded from Secret:
  {{ .Values.admin.existingSecret }}

KubeDeck stores its local D1 database in:
  {{ include "kubedeck.fullname" . }}-data
