apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubedeck-agent.fullname" . }}
  labels:
    {{- include "kubedeck-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kubedeck-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
